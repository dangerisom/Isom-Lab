# macOS Finder metadata
.DS_Store

# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Editor / IDE noise
.vscode/
*.swp
*~

# Python bytecode (in case any .py artifacts slip in)
__pycache__/
*.pyc

# --- Secrets: never commit credentials -----------------------------------
# Token / credential files (rotate on GitHub if one ever lands here)
*token*.txt
*tokens*.txt
*secret*
*credential*
*.pem
.env
.env.*
personal_access_token*
githubTokens*
